{
  "name": "import_export",
  "version": "1.0.0",
  "description": "---",
  "main": "index.js",
  "scripts": {
    "serve": "http-server",
    "live": "live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "BudTon",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  }
}
